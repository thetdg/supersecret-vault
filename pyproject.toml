[project]
name = "supersecret-vault"
version = "0.1.0"
description = "A vault for storing encrypted files"
requires-python = ">=3.13"
dependencies = [
    "pyinstaller==6.19.0",
    "pyqt6==6.10.2",
]
